#Experiment goal: Show the effect of batch arrivals with 3 small networks in the batch
# The networks are: AgeNet, GenderNet, FaceNet

n_workers: [1]
ait-multiplier: [0.8, 1, 1.2]
modes: [masa, bulk, deepeye-frugal]
memory-constraints: [2G, 1G, 512M]
networks: [FaceNet, ObjectNet, EmotionNet, MemNet, AgeNet, GenderNet, TinyYolo, SceneNet, SoS]
experiment-tag: all-random-rpi-4
device: rpi-4
